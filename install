 #!/bin/bash

 ln -sf $PWD/bash_config $HOME/.bash_aliases
 ls -l $HOME/.bash_aliases

 ln -sf $PWD/nvim_config $HOME/.config/nvim/init.vim
 ls -l $HOME/.config/nvim/init.vim

 ln -sf $PWD/tmux.conf $HOME/.tmux.conf
 ls -l $HOME/.tmux.conf
